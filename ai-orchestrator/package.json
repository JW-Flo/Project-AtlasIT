{
  "name": "atlasit-ai-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step (pure JS)'",
    "test": "vitest run || echo 'No orchestrator tests defined'"
  },
  "dependencies": {
    "@sveltejs/adapter-cloudflare": "^7.2.3",
    "@sveltejs/kit": "^2.43.7",
    "esbuild": "^0.25.10",
    "hono": "^4.3.0"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.40.3"
  }
}
