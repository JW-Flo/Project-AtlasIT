{
	"name": "atlasit-github-proxy",
	"version": "1.0.0",
	"description": "Secure Cloudflare Worker proxy for Codex-to-GitHub integration",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "^3.2.4",
		"wrangler": "^4.15.2"
	}
}
