{
  "name": "@atlasit/cdt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "tsc -p .",
    "test": "vitest run",
    "typecheck": "tsc -p . --noEmit",
    "lint": "eslint . --ext .ts",
    "fmt": "prettier -w ."
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "esbuild": "^0.21.5",
    "prettier": "^3.3.3",
    "eslint": "^8.57.0"
  },
  "dependencies": {}
}
