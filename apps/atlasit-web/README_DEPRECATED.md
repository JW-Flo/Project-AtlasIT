# Deprecated: atlasit-web

This directory previously contained experimental SvelteKit pages (access requests, incidents) now migrated into `console-app` for deployment.

Active UI lives in `console-app/src/routes`.

Safe to remove after confirming no remaining references.
