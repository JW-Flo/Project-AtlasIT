// Skeleton utility classes. Import once in a root style scope if needed.
// Exporting constants for semantic usage without adding runtime weight.

export const skeleton = {
  line: "sk sk-line",
  circle: "sk sk-circle",
  block: "sk sk-block",
};

// (Optional) style injection pattern could be added later if we want dynamic theming.
