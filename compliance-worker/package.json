{
  "name": "compliance-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "echo 'Cloudflare builds during deploy'",
    "test": "vitest run",
    "typecheck": "tsc -p . --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@cloudflare/workers-types": "^4.20250927.0",
    "wrangler": "^4.40.2",
    "vitest": "^4.1.0"
  }
}
