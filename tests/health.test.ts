import { describe, it, expect } from "vitest";

// Simple placeholder test – later can spin up wrangler dev and fetch

describe("placeholder", () => {
  it("adds numbers", () => {
    expect(1 + 1).toBe(2);
  });
});
